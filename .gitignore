node_modules/
.env
data/users.json

# Non-site files (scrapers, temp, drafts)
gitignore/

# Session store
data/sessions/

# Audit log (runtime generated)
data/audit.json

# Backup artifacts
backups/

# Upload working directories
uploads/.tmp/
uploads/.quarantine/

# Ignore uploaded images (keep .gitkeep for dir tracking)
uploads/*
!uploads/.gitkeep
